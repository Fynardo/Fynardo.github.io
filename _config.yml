# Welcome to Jekyll!
#
# Site settings
title: La imprenta de Fynardo
name: La imprenta de Fynardo
email: dfynar@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  Datos e Ingeniería.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: Fynardo
github_username:  Fynardo

# Category pages
collections:
    category:
        output: true

defaults:
    -
        scope:
            path: ""
            type: category
        values:
            layout: "category"

# show_excerpts: true
date_format: "%b %-d, %Y"

# Build settings
markdown: kramdown
highlighter: rouge

kramdown:
    input: GFM
    syntax_highlighter: rouge

#theme: minima
#plugins:
#  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

sass:
    style: :compressed
    sass_dir: assets/css
